/**
 * - Recipe app UI
 * - Search bar
 * - Save functionality
 * - Animations (3d)
 */

import.meta.glob("./components/*", { eager: true });

setTimeout(async () => {
  await import("./three/main");
}, 3000);
